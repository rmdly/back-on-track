<% content_for :title, "New workout template" %>
<header class="mb-6">
  <%= link_to "← Templates", training_workout_templates_path, class: "text-sm text-gray-500 hover:text-gray-900" %>
  <h1 class="mt-2 text-2xl font-bold tracking-tight">New workout template</h1>
</header>

<%= render "form", workout_template: @workout_template, url: training_workout_templates_path, submit_label: "Create template" %>
