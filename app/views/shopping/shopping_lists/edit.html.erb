<% content_for :title, "Edit shopping list" %>
<header class="mb-6">
  <%= link_to "← Back to list", shopping_shopping_list_path(@shopping_list), class: "text-sm text-gray-500 hover:text-gray-900" %>
  <h1 class="mt-2 text-2xl font-bold tracking-tight">Edit shopping list</h1>
</header>

<%= render "form", shopping_list: @shopping_list, url: shopping_shopping_list_path(@shopping_list), submit_label: "Save changes" %>
